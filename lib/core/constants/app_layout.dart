// lib/core/constants/app_layout.dart

class AppLayout {
  // Spacing
  static const double spaceXS = 4.0;
  static const double spaceS = 8.0;
  static const double spaceM = 16.0;
  static const double spaceL = 24.0;
  static const double spaceXL = 32.0;

  // Border Radii
  static const double radiusS = 8.0;
  static const double radiusM = 12.0;
  static const double radiusL = 16.0;
  static const double radiusXL = 24.0;

  // Durations
  static const Duration animShort = Duration(milliseconds: 200);
  static const Duration animMedium = Duration(milliseconds: 400);

  // Layout Constraints
  static const double maxContentWidth = 1200.0;
  static const double inspectorWidth = 350.0;
  static const double sidebarMinWidth = 250.0;
  static const double sidebarMaxWidth = 320.0;
}
